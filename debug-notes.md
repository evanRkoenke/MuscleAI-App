# Debug Notes

## Stale TSC Watcher Issue
The health check panel shows errors referencing server/sync.ts — a file that does NOT exist in this version. Fresh tsc --noEmit returns 0 errors.

## Preview Status
Dashboard renders correctly with Sugar tracking column visible in macro row.

## Test Results
101 tests passing, 0 TypeScript errors.
